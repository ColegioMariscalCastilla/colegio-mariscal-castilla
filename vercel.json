{
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install --force",
  "framework": "vite",
  "functions": {
    "server/index.ts": {
      "maxDuration": 30
    }
  }
}
